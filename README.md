# Windows Desktop Harness

A small Windows desktop UI test harness used to exercise common interaction patterns such as file selection, drag and drop, progress, cancellation, result presentation, settings, record browsing, and PDF export.

The backend is intentionally synthetic. It performs no domain-specific processing and contains no production logic.
