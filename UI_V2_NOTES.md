# UI V2

This branch validates a simplified single-page desktop workflow with two stacked work areas: one for creating output files and one for inspecting a file and exporting a PDF report. The implementation remains synthetic and generic.
