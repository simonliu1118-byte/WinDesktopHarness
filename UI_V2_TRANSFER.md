# UI V2 transfer notes

Only generic Windows UI behavior is tested here. Approved layout and interaction patterns must remain transferable to the private production application's existing Windows UI and state flow. Any prototype behavior that would require redesigning the private core must be rejected before implementation.
